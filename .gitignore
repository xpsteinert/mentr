# Downloaded Ollama models — regenerated by the init Job, kept locally
# so `k3d cluster delete` + recreate doesn't force a multi-GB re-download.
# Directory itself is tracked (via .gitkeep) so it exists after a fresh clone.
.ollama-models/*
!.ollama-models/.gitkeep

# OS / editor cruft
.DS_Store
*.swp
