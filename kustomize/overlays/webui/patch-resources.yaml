apiVersion: apps/v1
kind: Deployment
metadata:
  name: mentr-webui
  namespace: ai-stack
spec:
  template:
    spec:
      containers:
        - name: webui
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
