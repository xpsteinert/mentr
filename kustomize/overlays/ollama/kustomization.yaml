apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/ollama
  - pv-models.yaml
  # - nvidia-device-plugin.yaml  # GPU-only host — uncomment along with the
  #   nvidia.com/gpu resource lines in patch-resources.yaml to enable

patchesStrategicMerge:
  - patch-pvc.yaml
  - patch-resources.yaml
  - patch-initjob-resources.yaml
