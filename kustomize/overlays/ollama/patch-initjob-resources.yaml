apiVersion: batch/v1
kind: Job
metadata:
  name: mentr-ollama-model-init
  namespace: ai-stack
spec:
  template:
    spec:
      containers:
        - name: ollama-pull
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
