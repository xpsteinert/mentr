apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied to every object below — used by the "overlays" resource
# patches to target objects consistently.
commonLabels:
  app.kubernetes.io/part-of: mentr

resources:
  - namespace
  - ollama
  - webui
